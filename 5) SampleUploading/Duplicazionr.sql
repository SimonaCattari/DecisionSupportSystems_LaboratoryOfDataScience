-- Duplica tabelle SSIS(vuote)
SELECT TOP 0 *
INTO date_SSIS
FROM date

SELECT TOP 0 *
INTO geography_SSIS
FROM geography

SELECT TOP 0 *
INTO matchFact_SSIS
FROM match_fact

SELECT TOP 0 *
INTO matchStats_SSIS
FROM match_stats

SELECT TOP 0 *
INTO player_SSIS
FROM player

SELECT TOP 0 *
INTO tournament_SSIS
FROM tournament

-- Duplica tabelle SSIS (python)
SELECT TOP 0 *
INTO date_SSIS_PY
FROM date

SELECT TOP 0 *
INTO geography_SSIS_PY
FROM geography

SELECT TOP 0 *
INTO matchFact_SSIS_PY
FROM match_fact

SELECT TOP 0 *
INTO matchStats_SSIS_PY
FROM match_stats

SELECT TOP 0 *
INTO player_SSIS_PY
FROM player

SELECT TOP 0 *
INTO tournament_SSIS_PY
FROM tournament

